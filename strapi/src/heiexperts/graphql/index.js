const { resolvers } = require("./resolvers");
const { resolversConfig } = require("./resolversConfig");
const { typeDefs } = require("./types");

module.exports = { resolvers, resolversConfig, typeDefs };

